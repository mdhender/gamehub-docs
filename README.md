# EC Docs

## Project Layout

A simple mental model for that structure:

    docs/players    = modern player-facing docs
    docs/referees   = operator / GM material
    docs/developers = the dense coding-agent manual, split into useful pieces
    history         = preserved editions, kept close to the originals
    reference       = cross-cutting quick lookup pages
